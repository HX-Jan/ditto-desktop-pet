# v0.1.0 验证记录

## 环境

- 日期：2026-09-16。
- 本机：Windows 11 24H2，操作系统内部版本 26100，x64，主屏 2560×1600，175% 缩放。
- SDK：10.0.401。免安装包内含 .NET / Windows Desktop Runtime 10.0.12。
- 验证结果仅代表下列实际执行的检查，不代表全部 Windows 10/11 设备。

## 已完成

- Release 编译：零警告、零错误。
- 核心行为检查：31 项通过，覆盖点击与拖动阈值、下落中点击、睡眠、暂停拖动、隐藏、边界、配置恢复。
- 100%、125%、150%、200% 的工作区坐标：各模拟 30 分钟移动，无越界。这是逻辑测试，并非四档系统缩放的实机测试。
- WPF 原生窗口检查：透明角落命中后方窗口、不透明身体可交互、非激活样式、置顶、工具窗口、三档实际尺寸、暂停冻结、睡眠、拖放落地、隐藏停止计时和恢复。
- 当前系统 175% 缩放：已观察桌面实际显示；宠物位于任务栏上方，透明背景无矩形底色。
- 运行时模块路径检查：`hostfxr.dll`、`hostpolicy.dll`、`coreclr.dll`、`PresentationNative_cor3.dll` 均从免安装包目录加载。
- ZIP 已解压到独立临时目录，在将 `DOTNET_ROOT` 指向不存在目录并关闭多级查找的环境中启动，17 项桌面集成检查全部通过；主机追踪也确认使用包内运行时。
- 使用真实右键菜单「隐藏到托盘」后，再次启动程序可恢复原实例；第二个进程退出，正常关闭主实例后进程结束。

## 发布前进行中的检查

- 免安装程序的 30 分钟实际运行与资源采样。
- GitHub Actions 构建与 Release 附件完整性。

## 尚未实际验证

- 未安装 .NET 的全新 Windows 或隔离虚拟机；本机已确认使用包内运行时，但不将此称为干净系统实测。
- Windows 10 实机、不同显卡／远程桌面、主屏切换、任务栏位置改变、物理分辨率变化。
- 将 Windows 系统实际切换到 100%、125%、150%、200% 后的视觉和输入效果。
- 长期数日运行与资源占用。

## 已知限制

- 只在主显示器工作区内活动；不支持跨屏拖动。
- 非整数系统缩放下，最近邻像素绘制可能出现像素宽度不均。
- 没有自动全屏避让，需要时可手动隐藏。
- 未进行代码签名；下载启动可能出现未知发布者提示。
- 没有自动更新，升级需退出后重新解压新版本。
